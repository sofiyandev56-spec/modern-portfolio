# The public URL of the deployed site. Used for metadataBase so that link
# previews (Open Graph / Twitter cards) resolve to absolute URLs.
#
# On Vercel this is detected automatically and you can leave it unset.
# Set it explicitly if you use a custom domain or deploy elsewhere.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
